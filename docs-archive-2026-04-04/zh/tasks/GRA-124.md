---
linear-id: GRA-124
title: "[Indexer] Upgrade mock to real service with PostgreSQL"
status: done
priority: P1
project: "Chain Hub Indexer"
created: 2026-04-03
assignee: "Code Agent"
tags: [task, p1, indexer, phase-1]
---

# GRA-124: [Indexer] Upgrade mock to real service with PostgreSQL

## Description
Replace in-memory Maps in `packages/indexer-mock/` with PostgreSQL-backed persistence while keeping the same API contract (Hono routes, WebSocket events).

## Steps
1. Add PostgreSQL client (pg / Prisma / Drizzle - match project conventions)
2. Refactor `src/index.ts` routes to use DB queries instead of Maps
3. Maintain same API endpoints: `/api/tasks`, `/api/agents`, `/api/judge-pool`, `/api/discover`, `/api/stats`
4. Keep WebSocket broadcast functionality
5. Add connection pooling and error handling
6. Migration from seed data to DB
7. Update tests

## Acceptance Criteria
- [ ] All existing API endpoints return same response shape
- [ ] Data persists across restarts
- [ ] WebSocket events still work
- [ ] AgentM frontend works without changes
- [ ] Performance: < 100ms for common queries

## Related
- [[GRA-123]] Depends on PostgreSQL infrastructure
- [[packages/indexer-mock/src/index.ts]] Current implementation

## Log
- 2026-04-03: Created from gap closure plan
