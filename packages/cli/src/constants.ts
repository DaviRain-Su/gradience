/**
 * CLI Constants
 * 
 * Shared constants used across CLI commands
 */

// Time constants (in seconds)
export const SECONDS_PER_DAY = 86_400n;
export const DEFAULT_TASK_DURATION_DAYS = 1n;

// Default values
export const DEFAULT_JUDGE_MODE = 'pool';
export const DEFAULT_CATEGORY = 0;
export const DEFAULT_MIN_STAKE = 0n;
