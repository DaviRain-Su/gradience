name: E2E Tests

# Disabled: E2E tests need proper local services to run.
# Re-enable when e2e/ tests are updated for current architecture.
on:
  workflow_dispatch:

jobs:
  e2e-test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "E2E tests are currently disabled. Trigger manually when ready."
