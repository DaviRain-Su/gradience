---
linear-id: GRA-227
title: '[Web] ERC-8004 Agent Registration & Reputation Display'
status: todo
priority: P1
project: 'AgentM Web'
created: 2026-04-05
assignee: ''
tags: [task, p1, web, erc-8004, reputation, ui]
---

# GRA-227: ERC-8004 Web Integration

## Description

在 AgentM Web 中集成 ERC-8004 协议，实现:

1. Agent 注册到 ERC-8004 Identity Registry
2. 显示 ERC-8004 链上声誉
3. 提交声誉反馈

## Implementation Tasks

### GRA-227a: ERC-8004 Registration UI

**文件**: `apps/agentm-web/src/components/erc8004/RegistrationCard.tsx`

功能:

- 在 Agent 创建流程中添加 ERC-8004 注册步骤
- 生成 ERC-8004 注册 JSON (TODO T49)
- 提交到 Daemon `/api/v1/reputation/erc8004/register`

**UI 设计**:

```
┌─ ERC-8004 Registration ─┐
│                         │
│  Agent: @my-agent       │
│  Owner: 0x1234...       │
│                         │
│  [Register on-chain]    │
│                         │
│  Status: ✅ Registered  │
│  TX: 0xabcd...          │
│                         │
└─────────────────────────┘
```

**Acceptance Criteria**:

- [ ] 注册卡片组件
- [ ] 集成到 Agent 创建流程
- [ ] 显示注册状态
- [ ] 链接到 Etherscan

### GRA-227b: ERC-8004 Reputation Display

**文件**: `apps/agentm-web/src/components/erc8004/ReputationBadge.tsx`

功能:

- 显示 ERC-8004 链上声誉分数
- 显示声誉历史图表
- 显示跨链声誉状态

**Acceptance Criteria**:

- [ ] 声誉徽章组件
- [ ] 历史趋势图表
- [ ] 跨链声誉聚合显示

### GRA-227c: Reputation Feedback UI

**文件**: `apps/agentm-web/src/components/erc8004/FeedbackForm.tsx`

功能:

- 任务完成后提交声誉反馈 (TODO T50)
- 评分 + 评论
- 提交到 ERC-8004

**Acceptance Criteria**:

- [ ] 反馈表单组件
- [ ] 集成到任务完成流程
- [ ] 评分验证

### GRA-227d: Hooks

**文件**: `apps/agentm-web/src/hooks/useERC8004.ts`

```typescript
interface UseERC8004Result {
    // 查询 ERC-8004 声誉
    getReputation: (agentAddress: string) => Promise<ERC8004Reputation>;

    // 注册 Agent
    register: (agentData: AgentData) => Promise<string>;

    // 提交反馈
    giveFeedback: (feedback: FeedbackData) => Promise<string>;

    // 是否已注册
    isRegistered: boolean;

    // 加载状态
    loading: boolean;
}
```

**Acceptance Criteria**:

- [ ] useERC8004 hook 实现
- [ ] 错误处理
- [ ] 缓存优化

## API Integration

使用 Daemon API:

- `GET /api/v1/reputation/erc8004/:agentAddress`
- `POST /api/v1/reputation/erc8004/register`
- `POST /api/v1/reputation/erc8004/feedback`

## Dependencies

- GRA-226 (Daemon ERC-8004 integration)
- GRA-225 (Reputation Wallet)

## Related

- TODO T49: Generate ERC-8004 registration JSON
- TODO T50: Submit reputation feedback to 8004
