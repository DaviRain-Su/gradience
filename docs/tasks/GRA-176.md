---
linear-id: GRA-176
title: "[AgentM Web] 性能优化和测试"
status: todo
priority: P1
project: "AgentM Web"
created: 2026-04-04
assignee: ""
tags: [task, p1, agentm-web, testing, performance]
---

# GRA-176: [AgentM Web] 性能优化和测试 (P1)

## 描述
优化 json-render 集成的性能，编写完整的测试套件，确保稳定性和用户体验

## 背景
性能是 AI 生成 UI 的关键。需要确保首屏渲染快、交互流畅、AI 响应及时。同时需要完善的测试保证代码质量。

## 子任务

### 性能优化
- [ ] 实现 Spec 缓存
  - localStorage 缓存（客户端）
  - Redis 缓存（服务端）
  - 缓存 key 设计（hash prompt）
  - TTL 策略
- [ ] 添加组件懒加载
  - React.lazy 分割
  - 按需加载大型组件
  - 骨架屏 loading
- [ ] 首屏渲染优化
  - SSR 支持
  - 关键 CSS 内联
  - 图片优化
- [ ] AI 响应优化
  - 请求去重
  - 防抖处理
  - 超时重试

### 测试套件
- [ ] 单元测试
  - catalog.ts 测试
  - registry.tsx 测试
  - utils.ts 测试
  - AI prompt 测试
  - 覆盖率 > 70%
- [ ] 组件测试
  - SmartConfig 组件
  - DynamicDashboard 组件
  - Renderer 组件
  - 使用 React Testing Library
- [ ] 集成测试
  - API 接口测试
  - 数据流测试
  - 缓存测试
- [ ] E2E 测试
  - 完整用户流程
  - 使用 Playwright
  - 关键路径覆盖
- [ ] 性能测试
  - Lighthouse CI
  - 首屏时间
  - 交互响应时间

### 监控和日志
- [ ] 添加性能监控
  - Web Vitals
  - AI 响应时间追踪
  - 错误率监控
- [ ] 日志系统
  - 结构化日志
  - 错误上报
  - 用户行为追踪

## 验收标准
- [ ] 单元测试覆盖率 > 70%
- [ ] 首屏渲染时间 < 2s (Lighthouse)
- [ ] AI 响应缓存命中率 > 50%
- [ ] 所有 E2E 测试通过
- [ ] Lighthouse 性能评分 > 90
- [ ] 错误率 < 1%

## 性能指标

### 关键指标
| 指标 | 目标 | 测量方式 |
|------|------|----------|
| 首屏渲染 (FCP) | < 1.5s | Lighthouse |
| 可交互时间 (TTI) | < 3s | Lighthouse |
| AI 响应时间 | < 3s | 自定义追踪 |
| Spec 生成时间 | < 5s | 自定义追踪 |
| 缓存命中率 | > 50% | 监控数据 |

### 监控 Dashboard
```
┌─────────────────────────────────────────┐
│  📊 json-render Performance            │
├─────────────────────────────────────────┤
│                                         │
│  响应时间       缓存命中率      错误率   │
│  ┌───────┐     ┌───────┐     ┌───────┐ │
│  │ 2.3s  │     │ 65%   │     │ 0.5%  │ │
│  │ ↓ 10% │     │ ↑ 5%  │     │ → 0%  │ │
│  └───────┘     └───────┘     └───────┘ │
│                                         │
│  响应时间趋势 (过去 24h)                │
│  [████████████████████░░░░░░░░]         │
│                                         │
│  常用查询 TOP 5                         │
│  1. 收益分析 (234 次)                   │
│  2. 价格监控 (189 次)                   │
│  3. ...                                 │
│                                         │
└─────────────────────────────────────────┘
```

## 测试策略

### 单元测试示例
```typescript
// __tests__/json-render/catalog.test.ts
describe("AgentM Catalog", () => {
  it("should validate valid component specs", () => {
    const validSpec = {
      type: "Card",
      props: { title: "Test" },
    };
    expect(() => validateSpec(validSpec)).not.toThrow();
  });

  it("should reject invalid component types", () => {
    const invalidSpec = {
      type: "UnknownComponent",
      props: {},
    };
    expect(() => validateSpec(invalidSpec)).toThrow();
  });
});

// __tests__/ai/spec-generator.test.ts
describe("Spec Generator", () => {
  it("should generate agent config spec", async () => {
    const description = "Monitor ETH price";
    const spec = await generateSpec({
      prompt: description,
      type: "agent-config",
    });
    
    expect(spec.root).toBeDefined();
    expect(spec.elements).toHaveProperty("token-selector");
  });
});
```

### E2E 测试示例
```typescript
// e2e/smart-config.spec.ts
test("user can create agent with smart config", async ({ page }) => {
  await page.goto("/agents/create");
  
  // 切换到智能模式
  await page.click("[data-testid=mode-toggle]");
  
  // 输入描述
  await page.fill(
    "[data-testid=prompt-input]",
    "Monitor ETH price and alert when below 2000"
  );
  
  // 点击生成
  await page.click("[data-testid=generate-btn]");
  
  // 等待生成完成
  await page.waitForSelector("[data-testid=generated-form]");
  
  // 验证生成的表单
  await expect(page.locator("[data-testid=token-selector]")).toBeVisible();
  await expect(page.locator("[data-testid=price-input]")).toBeVisible();
});
```

## 工具配置

### Vitest 配置
```typescript
// vitest.config.ts
export default defineConfig({
  test: {
    coverage: {
      reporter: ["text", "html"],
      thresholds: {
        lines: 70,
        functions: 70,
        branches: 70,
        statements: 70,
      },
    },
  },
});
```

### Playwright 配置
```typescript
// playwright.config.ts
export default defineConfig({
  testDir: "./e2e",
  use: {
    baseURL: "http://localhost:3000",
    trace: "on-first-retry",
  },
  projects: [
    { name: "chromium", use: { ...devices["Desktop Chrome"] } },
    { name: "mobile", use: { ...devices["iPhone 12"] } },
  ],
});
```

## 参考
- [JSON_RENDER_INTEGRATION.md](../../apps/agentm-web/docs/JSON_RENDER_INTEGRATION.md) Phase 5

## 依赖
- [[GRA-173]] Agent 智能配置界面
- [[GRA-174]] 动态数据仪表盘

## 预估工期
3 天

## 相关任务
- [[GRA-175]] AI Playground

## Log
- 2026-04-04: 任务创建，标记为 P1
