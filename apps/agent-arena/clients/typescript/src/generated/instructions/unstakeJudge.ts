/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    getStructDecoder,
    getStructEncoder,
    getU8Decoder,
    getU8Encoder,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlyAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
    type WritableSignerAccount,
} from '@solana/kit';
import { findEventAuthorityPda } from '../pdas';
import { GRADIENCE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UNSTAKE_JUDGE_DISCRIMINATOR = 9;

export function getUnstakeJudgeDiscriminatorBytes() {
    return getU8Encoder().encode(UNSTAKE_JUDGE_DISCRIMINATOR);
}

export type UnstakeJudgeInstruction<
    TProgram extends string = typeof GRADIENCE_PROGRAM_ADDRESS,
    TAccountJudge extends string | AccountMeta<string> = string,
    TAccountStake extends string | AccountMeta<string> = string,
    TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
    TAccountEventAuthority extends string | AccountMeta<string> = string,
    TAccountGradienceProgram extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountJudge extends string
                ? WritableSignerAccount<TAccountJudge> & AccountSignerMeta<TAccountJudge>
                : TAccountJudge,
            TAccountStake extends string ? WritableAccount<TAccountStake> : TAccountStake,
            TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
            TAccountEventAuthority extends string ? ReadonlyAccount<TAccountEventAuthority> : TAccountEventAuthority,
            TAccountGradienceProgram extends string
                ? ReadonlyAccount<TAccountGradienceProgram>
                : TAccountGradienceProgram,
            ...TRemainingAccounts,
        ]
    >;

export type UnstakeJudgeInstructionData = { discriminator: number };

export type UnstakeJudgeInstructionDataArgs = {};

export function getUnstakeJudgeInstructionDataEncoder(): FixedSizeEncoder<UnstakeJudgeInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()]]), value => ({
        ...value,
        discriminator: UNSTAKE_JUDGE_DISCRIMINATOR,
    }));
}

export function getUnstakeJudgeInstructionDataDecoder(): FixedSizeDecoder<UnstakeJudgeInstructionData> {
    return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getUnstakeJudgeInstructionDataCodec(): FixedSizeCodec<
    UnstakeJudgeInstructionDataArgs,
    UnstakeJudgeInstructionData
> {
    return combineCodec(getUnstakeJudgeInstructionDataEncoder(), getUnstakeJudgeInstructionDataDecoder());
}

export type UnstakeJudgeAsyncInput<
    TAccountJudge extends string = string,
    TAccountStake extends string = string,
    TAccountSystemProgram extends string = string,
    TAccountEventAuthority extends string = string,
    TAccountGradienceProgram extends string = string,
> = {
    judge: TransactionSigner<TAccountJudge>;
    stake: Address<TAccountStake>;
    systemProgram?: Address<TAccountSystemProgram>;
    eventAuthority?: Address<TAccountEventAuthority>;
    gradienceProgram: Address<TAccountGradienceProgram>;
};

export async function getUnstakeJudgeInstructionAsync<
    TAccountJudge extends string,
    TAccountStake extends string,
    TAccountSystemProgram extends string,
    TAccountEventAuthority extends string,
    TAccountGradienceProgram extends string,
    TProgramAddress extends Address = typeof GRADIENCE_PROGRAM_ADDRESS,
>(
    input: UnstakeJudgeAsyncInput<
        TAccountJudge,
        TAccountStake,
        TAccountSystemProgram,
        TAccountEventAuthority,
        TAccountGradienceProgram
    >,
    config?: { programAddress?: TProgramAddress },
): Promise<
    UnstakeJudgeInstruction<
        TProgramAddress,
        TAccountJudge,
        TAccountStake,
        TAccountSystemProgram,
        TAccountEventAuthority,
        TAccountGradienceProgram
    >
> {
    // Program address.
    const programAddress = config?.programAddress ?? GRADIENCE_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        judge: { value: input.judge ?? null, isWritable: true },
        stake: { value: input.stake ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
        eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
        gradienceProgram: {
            value: input.gradienceProgram ?? null,
            isWritable: false,
        },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
    }
    if (!accounts.eventAuthority.value) {
        accounts.eventAuthority.value = await findEventAuthorityPda();
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.judge),
            getAccountMeta(accounts.stake),
            getAccountMeta(accounts.systemProgram),
            getAccountMeta(accounts.eventAuthority),
            getAccountMeta(accounts.gradienceProgram),
        ],
        data: getUnstakeJudgeInstructionDataEncoder().encode({}),
        programAddress,
    } as UnstakeJudgeInstruction<
        TProgramAddress,
        TAccountJudge,
        TAccountStake,
        TAccountSystemProgram,
        TAccountEventAuthority,
        TAccountGradienceProgram
    >);
}

export type UnstakeJudgeInput<
    TAccountJudge extends string = string,
    TAccountStake extends string = string,
    TAccountSystemProgram extends string = string,
    TAccountEventAuthority extends string = string,
    TAccountGradienceProgram extends string = string,
> = {
    judge: TransactionSigner<TAccountJudge>;
    stake: Address<TAccountStake>;
    systemProgram?: Address<TAccountSystemProgram>;
    eventAuthority: Address<TAccountEventAuthority>;
    gradienceProgram: Address<TAccountGradienceProgram>;
};

export function getUnstakeJudgeInstruction<
    TAccountJudge extends string,
    TAccountStake extends string,
    TAccountSystemProgram extends string,
    TAccountEventAuthority extends string,
    TAccountGradienceProgram extends string,
    TProgramAddress extends Address = typeof GRADIENCE_PROGRAM_ADDRESS,
>(
    input: UnstakeJudgeInput<
        TAccountJudge,
        TAccountStake,
        TAccountSystemProgram,
        TAccountEventAuthority,
        TAccountGradienceProgram
    >,
    config?: { programAddress?: TProgramAddress },
): UnstakeJudgeInstruction<
    TProgramAddress,
    TAccountJudge,
    TAccountStake,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountGradienceProgram
> {
    // Program address.
    const programAddress = config?.programAddress ?? GRADIENCE_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        judge: { value: input.judge ?? null, isWritable: true },
        stake: { value: input.stake ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
        eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
        gradienceProgram: {
            value: input.gradienceProgram ?? null,
            isWritable: false,
        },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.judge),
            getAccountMeta(accounts.stake),
            getAccountMeta(accounts.systemProgram),
            getAccountMeta(accounts.eventAuthority),
            getAccountMeta(accounts.gradienceProgram),
        ],
        data: getUnstakeJudgeInstructionDataEncoder().encode({}),
        programAddress,
    } as UnstakeJudgeInstruction<
        TProgramAddress,
        TAccountJudge,
        TAccountStake,
        TAccountSystemProgram,
        TAccountEventAuthority,
        TAccountGradienceProgram
    >);
}

export type ParsedUnstakeJudgeInstruction<
    TProgram extends string = typeof GRADIENCE_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        judge: TAccountMetas[0];
        stake: TAccountMetas[1];
        systemProgram: TAccountMetas[2];
        eventAuthority: TAccountMetas[3];
        gradienceProgram: TAccountMetas[4];
    };
    data: UnstakeJudgeInstructionData;
};

export function parseUnstakeJudgeInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedUnstakeJudgeInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 5) {
        // TODO: Coded error.
        throw new Error('Not enough accounts');
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            judge: getNextAccount(),
            stake: getNextAccount(),
            systemProgram: getNextAccount(),
            eventAuthority: getNextAccount(),
            gradienceProgram: getNextAccount(),
        },
        data: getUnstakeJudgeInstructionDataDecoder().decode(instruction.data),
    };
}
