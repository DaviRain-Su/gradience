/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    getArrayDecoder,
    getArrayEncoder,
    getOptionDecoder,
    getOptionEncoder,
    getStructDecoder,
    getStructEncoder,
    getU64Decoder,
    getU64Encoder,
    getU8Decoder,
    getU8Encoder,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type Codec,
    type Decoder,
    type Encoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type Option,
    type OptionOrNullable,
    type ReadonlySignerAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
} from '@solana/kit';
import { GRADIENCE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPGRADE_CONFIG_DISCRIMINATOR = 10;

export function getUpgradeConfigDiscriminatorBytes() {
    return getU8Encoder().encode(UPGRADE_CONFIG_DISCRIMINATOR);
}

export type UpgradeConfigInstruction<
    TProgram extends string = typeof GRADIENCE_PROGRAM_ADDRESS,
    TAccountAuthority extends string | AccountMeta<string> = string,
    TAccountConfig extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountAuthority extends string
                ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
                : TAccountAuthority,
            TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig,
            ...TRemainingAccounts,
        ]
    >;

export type UpgradeConfigInstructionData = {
    discriminator: number;
    newTreasury: Option<Array<number>>;
    newMinJudgeStake: Option<bigint>;
};

export type UpgradeConfigInstructionDataArgs = {
    newTreasury: OptionOrNullable<Array<number>>;
    newMinJudgeStake: OptionOrNullable<number | bigint>;
};

export function getUpgradeConfigInstructionDataEncoder(): Encoder<UpgradeConfigInstructionDataArgs> {
    return transformEncoder(
        getStructEncoder([
            ['discriminator', getU8Encoder()],
            ['newTreasury', getOptionEncoder(getArrayEncoder(getU8Encoder(), { size: 32 }))],
            ['newMinJudgeStake', getOptionEncoder(getU64Encoder())],
        ]),
        value => ({ ...value, discriminator: UPGRADE_CONFIG_DISCRIMINATOR }),
    );
}

export function getUpgradeConfigInstructionDataDecoder(): Decoder<UpgradeConfigInstructionData> {
    return getStructDecoder([
        ['discriminator', getU8Decoder()],
        ['newTreasury', getOptionDecoder(getArrayDecoder(getU8Decoder(), { size: 32 }))],
        ['newMinJudgeStake', getOptionDecoder(getU64Decoder())],
    ]);
}

export function getUpgradeConfigInstructionDataCodec(): Codec<
    UpgradeConfigInstructionDataArgs,
    UpgradeConfigInstructionData
> {
    return combineCodec(getUpgradeConfigInstructionDataEncoder(), getUpgradeConfigInstructionDataDecoder());
}

export type UpgradeConfigInput<TAccountAuthority extends string = string, TAccountConfig extends string = string> = {
    authority: TransactionSigner<TAccountAuthority>;
    config: Address<TAccountConfig>;
    newTreasury: UpgradeConfigInstructionDataArgs['newTreasury'];
    newMinJudgeStake: UpgradeConfigInstructionDataArgs['newMinJudgeStake'];
};

export function getUpgradeConfigInstruction<
    TAccountAuthority extends string,
    TAccountConfig extends string,
    TProgramAddress extends Address = typeof GRADIENCE_PROGRAM_ADDRESS,
>(
    input: UpgradeConfigInput<TAccountAuthority, TAccountConfig>,
    config?: { programAddress?: TProgramAddress },
): UpgradeConfigInstruction<TProgramAddress, TAccountAuthority, TAccountConfig> {
    // Program address.
    const programAddress = config?.programAddress ?? GRADIENCE_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        authority: { value: input.authority ?? null, isWritable: false },
        config: { value: input.config ?? null, isWritable: true },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

    // Original args.
    const args = { ...input };

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [getAccountMeta(accounts.authority), getAccountMeta(accounts.config)],
        data: getUpgradeConfigInstructionDataEncoder().encode(args as UpgradeConfigInstructionDataArgs),
        programAddress,
    } as UpgradeConfigInstruction<TProgramAddress, TAccountAuthority, TAccountConfig>);
}

export type ParsedUpgradeConfigInstruction<
    TProgram extends string = typeof GRADIENCE_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        authority: TAccountMetas[0];
        config: TAccountMetas[1];
    };
    data: UpgradeConfigInstructionData;
};

export function parseUpgradeConfigInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedUpgradeConfigInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 2) {
        // TODO: Coded error.
        throw new Error('Not enough accounts');
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { authority: getNextAccount(), config: getNextAccount() },
        data: getUpgradeConfigInstructionDataDecoder().decode(instruction.data),
    };
}
