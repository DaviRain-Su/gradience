# Phase 3: Technical Spec（技术规格）

> **目的**: 将架构设计转化为可以直接编码的精确规格
> **输入**: Phase 2 架构文档
> **输出物**: 填写完成的本文档，存放到 `<project>/docs/03-technical-spec.md`
>
> ⚠️ **这是最重要的文档。代码必须与本规格 100% 一致。**
> ⚠️ **任何模糊之处都必须在这里解决，不能留给实现阶段。**

---

## 3.1 数据结构定义（必填）

> 定义所有数据结构，精确到字段名、类型、大小、约束。

### 3.1.1 链上数据结构（如适用）

> 对于 Solana Program / 智能合约，精确到字节。

```rust
// 示例格式：
/// 账户名称
/// 总大小: N bytes
/// PDA 种子: [b"prefix", key.as_ref(), &id.to_le_bytes()]
#[account]
pub struct ExampleAccount {
    /// 说明（必须有注释）
    pub field_name: Type,         // N bytes — 约束说明
}
```

**每个数据结构需要包含：**

| 字段          | 类型    | 大小 (bytes) | 约束        | 说明         |
| ------------- | ------- | ------------ | ----------- | ------------ |
| discriminator | [u8; 8] | 8            | Anchor 自动 | 账户类型标识 |
| field_1       | Pubkey  | 32           | 非空        | 说明         |
| field_2       | u64     | 8            | > 0         | 说明         |
| ...           |         |              |             |              |
| **总计**      |         | **N**        |             |              |

### 3.1.2 链下数据结构

> 数据库 Schema、缓存结构、消息格式等。

```sql
-- 示例：数据库表
CREATE TABLE example (
    id TEXT PRIMARY KEY,          -- 说明
    field_1 TEXT NOT NULL,        -- 说明
    field_2 INTEGER DEFAULT 0,   -- 说明
    created_at INTEGER NOT NULL   -- Unix timestamp
);
```

### 3.1.3 配置与常量

> 所有硬编码的值，集中定义。

| 常量名 | 值  | 类型 | 说明 | 可变性                   |
| ------ | --- | ---- | ---- | ------------------------ |
|        |     |      |      | immutable / configurable |

## 3.2 接口定义（必填）

> 所有对外接口的精确定义。

### 3.2.1 Program 指令 / 合约函数（如适用）

> 每个指令/函数一个表。

**指令: `instruction_name`**

| 属性     | 值                   |
| -------- | -------------------- |
| 调用者   | 谁可以调用           |
| 前置条件 | 调用前必须满足的状态 |
| 后置条件 | 调用后的状态变化     |

参数:

| 参数 | 类型 | 必填 | 约束 | 说明 |
| ---- | ---- | ---- | ---- | ---- |
|      |      |      |      |      |

账户列表 (Solana):

| 账户 | 类型 | mut   | signer | 说明 |
| ---- | ---- | ----- | ------ | ---- |
|      |      | ✅/❌ | ✅/❌  |      |

返回值:

| 返回 | 类型 | 说明 |
| ---- | ---- | ---- |
|      |      |      |

### 3.2.2 REST API（如适用）

> 每个端点一个定义。

**`METHOD /path`**

```
Request:
  Headers: { Authorization: "Bearer <token>" }
  Body: {
    "field": "type — 说明"
  }

Response 200:
  {
    "field": "type — 说明"
  }

Response 4xx/5xx:
  {
    "error": "error_code",
    "message": "human readable"
  }
```

### 3.2.3 SDK 公开接口（如适用）

```typescript
// 每个公开方法的类型签名 + JSDoc
interface ExampleSDK {
    /**
     * 说明
     * @param param1 — 说明
     * @returns 说明
     * @throws ErrorCode — 什么情况下
     */
    methodName(param1: Type1): Promise<ReturnType>;
}
```

### 3.2.4 事件 / 回调（如适用）

| 事件名 | 触发时机 | 数据格式          |
| ------ | -------- | ----------------- |
|        |          | `{ field: type }` |

## 3.3 错误码定义（必填）

> 所有可能的错误，统一编号。

| 错误码 | 名称 | 触发条件 | 用户提示 |
| ------ | ---- | -------- | -------- |
| 6000   |      |          |          |
| 6001   |      |          |          |

## 3.4 状态机精确定义（必填，如有状态）

> 将架构中的状态图转化为精确的转换规则。

| 当前状态 | 触发动作 | 条件 | 新状态 | 副作用 |
| -------- | -------- | ---- | ------ | ------ |
|          |          |      |        |        |

## 3.5 算法与计算（必填，如有）

> 任何非 trivial 的计算逻辑，写出伪代码或公式。

```
// 示例：费用计算
judge_fee = reward * JUDGE_FEE_BPS / 10000
protocol_fee = reward * PROTOCOL_FEE_BPS / 10000
agent_payout = reward - judge_fee - protocol_fee

// 精度处理：使用整数除法，向下取整
// 余数归属：agent（因为是最后计算）
```

## 3.6 安全规则（必填）

> 从安全架构文档中提取本项目相关的具体规则。

| 规则         | 实现方式                   | 验证方法    |
| ------------ | -------------------------- | ----------- |
| 例：防重入   | CEI 模式 + ReentrancyGuard | 测试 + 审计 |
| 例：权限校验 | has_one 约束               | 单元测试    |

## 3.7 PDA / 地址推导（必填，Solana 项目）

> 所有 PDA 的种子定义。

| PDA 用途 | 种子                                  | Bump   | 说明 |
| -------- | ------------------------------------- | ------ | ---- |
|          | `[b"prefix", key, &id.to_le_bytes()]` | stored |      |

## 3.8 升级与迁移（可选）

> 如果存在旧版本，如何迁移数据。

## 3.9 边界条件清单（必填）

> 列出所有需要特殊处理的边界情况。

| #   | 边界条件     | 预期行为 | 备注 |
| --- | ------------ | -------- | ---- |
| 1   | 输入为 0     |          |      |
| 2   | 输入为最大值 |          |      |
| 3   | 并发操作     |          |      |
| 4   | 余额不足     |          |      |

---

## ✅ Phase 3 验收标准

- [ ] 所有数据结构精确到字段类型和字节大小
- [ ] 所有接口有完整的参数、返回值、错误码定义
- [ ] 错误码统一编号，无遗漏
- [ ] 状态机转换条件精确，无歧义
- [ ] 所有计算有伪代码/公式，精度处理已说明
- [ ] 安全规则已从架构文档映射到具体实现
- [ ] PDA 种子无冲突
- [ ] 边界条件已列出（至少 10 个）
- [ ] 本文档可以直接交给任何开发者（或 AI），不需要额外口头解释即可实现

**验收通过后，进入 Phase 4: Task Breakdown →**
